window.__OHARA_PARTS=window.__OHARA_PARTS||[];window.__OHARA_PARTS.push("d=\"owner\" value=\"${escapeHtml(value.owner)}\" placeholder=\"Ф.И.О. или роль\"></div>\n            <div class=\"field\"><label for=\"backup-${task.id}\">Замена на выходной / невыход</label><input id=\"backup-${task.id}\" data-task-field=\"backup\" value=\"${escapeHtml(value.backup)}\" placeholder=\"Ф.И.О. или старший следующей смены\"></div>\n            <div class=\"field full\"><label for=\"evidence-${task.id}\">Фиксация результата</label><textarea id=\"evidence-${task.id}\" data-task-field=\"evidence\" placeholder=\"${escapeHtml(task.evidence)}\">${escapeHtml(value.evidence)}</textarea></div>\n            ${task.rehearsal ? `<div class=\"field full\"><div class=\"rehearsal-row\"><input id=\"rehearsal-${task.id}\" type=\"checkbox\" data-task-field=\"rehearsal\" ${value.rehearsal?\"checked\":\"\"}><label for=\"rehearsal-${task.id}\">Ответственный выполнил процедуру самостоятельно, директор только наблюдал и дал обратную связь после выполнения.</label></div></div>` : \"\"}\n          </div>\n          <div class=\"task-validation\" role=\"status\"></div>\n          <div class=\"procedure-line\"><span><strong>Где посмотреть:</strong> ${escapeHtml(task.guide)}</span><button class=\"btn btn-small btn-ghost\" type=\"button\" data-open-tab=\"memos\">Открыть памятки →</button></div>\n        </div>\n      </article>`;\n    }\n\n    function bindTaskEvents() {\n      document.querySelectorAll(\".task-toggle\").forEach(button => button.addEventListener(\"click\", () => {\n        const task = button.closest(\".task\");\n        const open = task.classList.toggle(\"open\");\n        button.setAttribute(\"aria-expanded\", String(open));\n      }));\n      document.querySelectorAll(\"[data-task-field]\").forEach(control => {\n        const eventName = control.type === \"checkbox\" || control.tagName === \"SELECT\" ? \"change\" : \"input\";\n        control.addEventListener(eventName, onTaskFieldChange);\n      });\n    }\n\n    function onTaskFieldChange(event) {\n      const taskEl = event.target.closest(\".task\");\n      const id = taskEl.dataset.taskId;\n      const field = event.target.dataset.taskField;\n      const value = event.target.type === \"checkbox\" ? event.target.checked : event.target.value;\n      state.tasks[id][field] = value;\n      if (field === \"status\") event.target.dataset.status = value;\n      scheduleSave();\n      updateAllComputed();\n    }\n\n    function renderFinalChecks() {\n      el(\"finalChecks\").innerHTML = finalItems.map(item => `\n        <div class=\"final-item\">\n          <input id=\"${item.id}\" type=\"checkbox\" data-final-id=\"${item.id}\" ${state.final[item.id]?\"checked\":\"\"}>\n          <label for=\"${item.id}\">${escapeHtml(item.text)}</label>\n          <span class=\"system-hint\" data-final-hint=\"${item.hint}\">проверим</span>\n        </div>`).join(\"\");\n      document.querySelectorAll(\"[data-final-id]\").forEach(input => input.addEventListener(\"change\", e => {\n        state.final[e.target.dataset.finalId] = e.target.checked;\n        scheduleSave();\n        updateAllComputed();\n      }));\n    }\n\n    function renderRoutine() {\n      el(\"routineGrid\").innerHTML = routines.map(r => {\n        const value = state.routine[r.id];\n        return `<article class=\"routine-item\" data-routine-id=\"${r.id}\">\n          <div class=\"routine-icon\" aria-hidden=\"true\">${r.icon}</div>\n          <div><h4>${escapeHtml(r.title)}</h4><p>${escapeHtml(r.text)}</p>\n            <div class=\"routine-fields\">\n              <label><span class=\"visually-hidden\">Ответственный: ${escapeHtml(r.title)}</span><input data-routine-field=\"owner\" value=\"${escapeHtml(value.owner)}\" placeholder=\"Ответственный\"></label>\n              <label><span class=\"visually-hidden\">Где фиксируем: ${escapeHtml(r.title)}</span><input data-routine-field=\"record\" value=\"${escapeHtml(value.record)}\" placeholder=\"Где фиксируем\"></label>\n            </div>\n          </div>\n        </article>`;\n      }).join(\"\");\n      document.querySelectorAll(\"[data-routine-field]\").forEach(input => input.addEventListener(\"input\", e => {\n        const id = e.target.closest(\"[data-routine-id]\").dataset.routineId;\n        state.routine[id][e.target.dataset.routineField] = e.target.value;\n        scheduleSave();\n      }));\n    }\n\n    function renderScenarios() {\n      el(\"scenarioTable\").innerHTML = scenarios.map(s => `\n        <div class=\"scenario-row\" data-scenario-id=\"${s.id}\">\n          <div class=\"scenario-if\"><span>ЕСЛИ</span> ${escapeHtml(s.condition)}</div>\n          <label><span class=\"visually-hidden\">Действие для ситуации: ${escapeHtml(s.condition)}</span><textarea data-scenario-field=\"action\">${escapeHtml(state.scenario[s.id])}</textarea></label>\n        </div>`).join(\"\");\n      document.querySelectorAll(\"[data-scenario-field]\").forEach(input => input.addEventListener(\"input\", e => {\n        const id = e.target.closest(\"[data-scenario-id]\").dataset.scenarioId;\n        state.scenario[id] = e.target.value;\n        scheduleSave();\n      }));\n    }\n\n    function renderContacts() {\n      el(\"contactTable\").innerHTML = `<div class=\"contact-row head\"><div>Ситуация</div><div>Первый контакт</div><div>Второй контакт</div><div>Канал</div></div>` + contacts.map(c => {\n        const value = state.contact[c.id];\n        return `<div class=\"contact-row\" data-contact-id=\"${c.id}\">\n          <div class=\"contact-situation\">${escapeHtml(c.situation)}</div>\n          <label><span class=\"visually-hidden\">Первый контакт: ${escapeHtml(c.situation)}</span><input data-contact-field=\"first\" value=\"${escapeHtml(value.first)}\"></label>\n          <label><span class=\"visually-hidden\">Второй контакт: ${escapeHtml(c.situation)}</span><input data-contact-field=\"second\" value=\"${escapeHtml(value.second)}\"></label>\n          <label><span class=\"visually-hidden\">Канал связи: ${escapeHtml(c.situation)}</span><input data-contact-field=\"channel\" value=\"${escapeHtml(value.channel)}\"></label>\n        </div>`;\n      }).join(\"\");\n      document.querySelectorAll(\"[data-contact-field]\").forEach(input => input.addEventListener(\"input\", e => {\n        const id = e.target.closest(\"[data-contact-id]\").dataset.contactId;\n        state.contact[id][e.target.dataset.contactField] = e.target.value;\n        scheduleSave();\n        updateAllComputed();\n      }));\n    }\n\n    function renderProcedures() {\n      el(\"procedureTableBody\").innerHTML = procedures.map((p,i) => {\n        const id = \"pr\"+i;\n        const value = state.procedure[id];\n        return `<tr data-procedure-id=\"${id}\"><td><strong>${escapeHtml(p[0])}</strong></td><td><label><span class=\"visually-hidden\">Где смотреть: ${escapeHtml(p[0])}</span><input data-procedure-field=\"place\" value=\"${escapeHtml(value.place)}\"></label></td><td><label><span class=\"visually-hidden\">Кому обращаться: ${escapeHtml(p[0])}</span><input data-procedure-field=\"contact\" value=\"${escapeHtml(value.contact)}\"></label></td></tr>`;\n      }).join(\"\");\n      document.querySelectorAll(\"[data-procedure-field]\").forEach(input => input.addEventListener(\"input\", e => {\n        const id = e.target.closest(\"[data-procedure-id]\").dataset.procedureId;\n        state.procedure[id][e.target.dataset.procedureField] = e.target.value;\n        scheduleSave();\n        updateAllComputed();\n      }));\n    }\n\n    function taskValidation(task) {\n      const value = state.tasks[task.id];\n      if (value.status === \"not_applicable\") {\n        return value.evidence.trim() ? { valid:true, complete:false, applicable:false, missing:[] } : { valid:false, complete:false, applicable:false, missing:[\"пояснение, почему пункт не применим\"] };\n      }\n      if (value.status !== \"done\") return { valid:true, complete:false, applicable:true, missing:[] };\n      const missing = [];\n      if (!value.owner.trim()) missing.push(\"ответственный\");\n      if (!value.backup.trim()) missing.push(\"замена\");\n      if (!value.evidence.trim()) missing.push(\"фиксация результата\");\n      if (task.rehearsal && !value.rehearsal) missing.push(\"самостоятельная репетиция\");\n      return { valid:missing.length===0, complete:missing.length===0, applicable:true, missing };\n    }\n\n    function metrics() {\n      const result = { applicable:0, complete:0, percent:0, criticalIncomplete:[], invalid:[], byCategory:{} };\n      categories.forEach(c => result.byCategory[c.id] = { applicable:0, complete:0, percent:0 });\n      tasks.forEach(task => {\n        const validation = taskValidation(task);\n        if (validation.applicable) {\n          result.applicable += 1;\n          result.byCategory[task.category].applicable += 1;\n          if (validation.complete) {\n            result.complete += 1;\n            result.byCategory[task.category].complete += 1;\n          }\n        }\n        if (!validation.valid) result.invalid.push({ task, missing:validation.missing });\n        if (task.critical && !(validation.complete || !validation.applicable)) result.criticalIncomplete.push(task);\n      });\n      result.percent = result.applicable ? Math.round(result.complete / result.applicable * 100) : 100;\n      Object.values(result.byCategory).forEach(m => m.percent = m.applicable ? Math.round(m.complete / m.applicable * 100) : 100);\n      return result;\n    }\n\n    function readinessLevel(m) {\n      if (m.percent >= 90 && m.criticalIncomplete.length === 0) return \"green\";\n      if (m.percent >= 75 && m.criticalIncomplete.length <= 2) return \"yellow\";\n      return \"red\";\n    }\n\n    function updateAllComputed() {\n      const m = metrics();\n      updateTaskValidation();\n      updateDashboard(m);\n      updateCategoryProgress(m);\n      updateFinalHints(m);\n      updateFinalStamp(m);\n      updateHandover(m);\n      updatePrintSheet(m);\n    }\n\n    function updateTaskValidation() {\n      tasks.forEach(task => {\n        const validation = taskValidation(task);\n        const node = document.querySelector(`[data-task-id=\"${task.id}\"]`);\n        if (!node) return;\n        node.classList.toggle(\"invalid\", !validation.valid);\n        const box = node.querySelector(\".task-validation\");\n        if (!validation.valid) {\n          box.textContent = state.tasks[task.id].status === \"not_applicable\"\n            ? \"Для статуса «Не применимо» добавьте короткое пояснение в поле фиксации.\"\n            : \"Статус «Готово» пока не засчитан. Не заполнено: \" + validation.missing.join(\", \") + \".\";\n        } else box.textContent = \"\";\n      });\n    }\n\n    function updateDashboard(m) {\n      const level = readinessLevel(m);\n      const ring = el(\"progressRing\");\n      ring.style.setProperty(\"--p\", m.percent);\n      ring.style.setProperty(\"--ring-color\", level === \"green\" ? \"var(--green)\" : level === \"yellow\" ? \"var(--amber)\" : \"var(--ohara-red)\");\n      ring.setAttribute(\"aria-valuenow\", String(m.percent));\n      el(\"progressNumber\").textContent = m.percent + \"%\";\n      el(\"topProgressFill\").style.width = m.percent + \"%\";\n      el(\"topProgressFill\").style.background = level === \"green\" ? \"var(--green)\" : level === \"yellow\" ? \"var(--amber)\" : \"var(--ohara-red)\";\n      el(\"topProgressLabel\").textContent = m.percent + \"%\";\n      el(\"completedCount\").textContent = m.complete;\n      el(\"applicableCount\").textContent = m.applicable;\n      el(\"criticalCount\").textContent = m.criticalIncomplete.length;\n      el(\"mobileProgressText\").textContent = m.percent + \"% готово\";\n      el(\"mobileCriticalText\").textContent = m.criticalIncomplete.length + \" критичных незакрыто\";\n      const status = el(\"readinessStatus\");\n      status.dataset.level = level;\n      status.textContent = level === \"green\" ? \"Предварительно готов\" : level === \"yellow\" ? \"Почти готов\" : \"Нужны действия\";\n      updateDaysHint();\n    }\n\n    function updateDaysHint() {\n      const start = state.meta.vacationStart;\n      if (!start) { el(\"daysHint\").textContent = \"Укажите дату отпуска — покажем оставшееся время.\"; return; }\n      const startDate = new Date(start + \"T12:00:00\");\n      const today = new Date();\n      today.setHours(12,0,0,0);\n      const days = Math.ceil((startDate - today) / 86400000);\n      if (days > 14) el(\"daysHint\").innerHTML = `До отпуска <strong>${days}</strong> дн. Начать активную подготовку — з");